#include "main.h"

/**
 * _islower - checks for lowercase character
 * Description: Checks if character is lower case. Prints 1 is lowercase, 0 if otherwise
 * Return: 1 if character is lowercase, 0 otherwise
*/

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
